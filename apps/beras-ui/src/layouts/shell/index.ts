import type { ComponentType } from 'react';
import type { AppShellProps } from '../../public/types';

// BU-P1-08: replace this contract stub before the final gate.
export const AppShell: ComponentType<AppShellProps> = () => {
  throw new Error('AppShell requires BU-P1-08');
};
AppShell.displayName = 'AppShellContractStub';
